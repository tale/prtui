---
default: minor
---

#### Search the description and the key reference

`/` now searches whichever panel is open, not just the diff and the tree.
`n`/`N` step the hits and the panel scrolls to keep the current one in view.
