---
default: minor
---

#### Copy a permalink, or open the pull request on the web

`y` copies a permalink to whatever the cursor is on — the line, the visual
span, or the conversation. It goes through the terminal's own clipboard, so it
works over SSH. `gx` opens the pull request in a browser.
