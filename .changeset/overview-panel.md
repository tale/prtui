---
default: minor
---

#### Read the pull request description without leaving the terminal

`o` opens the description and the discussion under it over the panes. It
scrolls with the same keys as everything else, and `esc` closes it. The
description was already fetched; there was previously nowhere to read it.
