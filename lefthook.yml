pre-commit:
  piped: true
  jobs:
    - name: fmt
      glob:
        - "*.rs"
        - "rustfmt.toml"
      run: cargo fmt --all
      stage_fixed: true

    - name: clippy
      glob:
        - "*.rs"
        - "Cargo.toml"
      run: cargo clippy --all-targets --all-features -- -D warnings
