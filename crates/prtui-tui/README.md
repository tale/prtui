# prtui TUI

`prtui-tui` contains prtui's provider-independent state machine, layout, and
rendering. It consumes only models from `prtui-core`; transport and provider
wire types do not cross this boundary.
