# prtui GitHub provider

`prtui-github` implements `prtui_core::Provider` using the GitHub CLI, REST
API, and GraphQL API. Wire types, authentication, pagination, and GitHub URL
shapes stay inside this crate.
